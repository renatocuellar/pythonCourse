# Modules
# Podemos importar ficheros desde fuera, en esta caso, 
# lo que hacemos es crear una nueva hoja de llamada module.py

import module
from module import sum_two_values

module.sum(5, 7, 18)
module.sum_two_values(5, 2)